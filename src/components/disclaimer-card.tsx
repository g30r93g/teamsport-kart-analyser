import { Card, CardHeader, CardTitle } from "./ui/card";

export default function DisclaimerCard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>This application is not affiliated with TeamSport.</CardTitle>
            </CardHeader>
        </Card>
    )
}
